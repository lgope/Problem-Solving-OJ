#include<stdio.h>
#include<string.h>

int main()
{
    int n;
    char ch[100];

    while(scanf("%d", &n) != EOF)
    {
        getchar();
        gets(ch);

        printf("I am Toorg!\n");
    }

    return 0;
}
